import TestA from "./components/TestA"

export default () => {

    return <div __cursorPointer>pageA

        {/* <div onClick={() => setT(true)}>error</div> */}

        <TestA />
        <progress value={10} max={100} />
    </div>
}